import { AfterDateFilterPipe } from './after-date-filter.pipe';

describe('AfterDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AfterDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
