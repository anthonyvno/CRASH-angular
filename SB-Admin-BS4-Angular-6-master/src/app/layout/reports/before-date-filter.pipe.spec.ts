import { BeforeDateFilterPipe } from './before-date-filter.pipe';

describe('BeforeDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BeforeDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
