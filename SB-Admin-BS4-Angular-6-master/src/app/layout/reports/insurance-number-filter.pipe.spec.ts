import { InsuranceNumberFilterPipe } from './insurance-number-filter.pipe';

describe('InsuranceNumberFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InsuranceNumberFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
