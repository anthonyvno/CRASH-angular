import { VehicleTypeFilterPipe } from './vehicle-type-filter.pipe';

describe('VehicleTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
